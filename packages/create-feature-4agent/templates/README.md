# create-feature-4agent

Bootstrap the Planner-Verifier-Implementer-Tester workflow in an existing repository.

```bash
npx create-feature-4agent@latest
```

Existing files are preserved. Use `--force` only after reviewing the files that would be replaced.
